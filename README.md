# Alarm Reminder App

Simple browser app that lets you choose an exact date/time and triggers a reminder notification when the time is reached.

## Run

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000`.
